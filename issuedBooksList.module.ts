export class issuedBooksList{
   


    constructor(public userName:string, public book: string, public quantity:any){
       this.userName= userName;
       this.book= book;
       this.quantity=quantity;
      
    }
}